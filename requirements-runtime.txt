# Runtime dependencies of the Coomi-on-Kimi agent (no test tooling).
# Pinned to the versions this build was verified against on 2026-09-12.
agent-client-protocol==0.12.1
mcp==2.2.0
aiohttp==3.14.3
rich==15.0.0
pyyaml==6.0.3
